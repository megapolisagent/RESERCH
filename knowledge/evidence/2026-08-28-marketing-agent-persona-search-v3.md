# Находки — эталоны роли AI Marketing Strategist, расширенный заход v3: мультиагентные фреймворки, опубликованные AI CMO-персоны, YouTube

Research brief от владельца (шаблон `knowledge/research-brief-template.md`). Цель: подтвердить или опровергнуть текущий вывод «полное совпадение роль+границы+память+делегирование нигде не найдено» — через источники, которые v1/v2 не покрыли или не довели до конца. v2 (task `ai-intelligence-persona-search-v2`, id `4f90ce68`) зависла в статусе `blocked`, evidence-файла не оставила — этот заход не продолжает её историю, начинает с той же цели заново.

**Responsibility Boundary Gate**: это research, не design. Отчёт называет найденные эталоны и прямо отвечает на вопрос брифа. Он не решает, что менять в `AI Marketing Strategist/HOME.md` — это отдельный, gated шаг владельца.

**Internal-context-first**: перед внешним поиском проверено, что уже сделано —
- `knowledge/EVIDENCE_INDEX.md` — прямого совпадения по мультиагентным фреймворкам/AI CMO-персонам не найдено.
- Коммит `5e22953` (ветка `worktree-marketing-strategist-benchmark`, не слита в `main`) — эталон AMA/CIM уже закрыт (2026-08-27), прочитан целиком, **не повторяется** в этом отчёте по прямому указанию брифа.
- Ветки `worktree-marketing-persona-search` и `worktree-marketing-agent-persona-search` (v1, v2) — проверены через `git log`: ни одна не содержит уникальных коммитов сверх `main`, то есть реально ничего не произвели. Начинать не от чего.

---

## Технический доступ — проверено, один источник реально не работает

- **`youtube-transcript` MCP** — числится подключённым (инструмент `mcp__youtube-transcript__get_transcript` виден и вызывается), но **фактически неработоспособен в этой сессии**: вызван 3 раза (реальный найденный видео-ID `TxF08ewBH54`, заведомо иноязычный формат `youtu.be/...`, и заведомо капшнированное видео `dQw4w9WgXcQ` для проверки, что дело не в конкретном видео) — во всех трёх случаях инструмент вернул буквальную строку `[object Object]`, не транскрипт. Это не «не нашлось», это поломка интеграции — честно фиксирую, не выдаю за «источник проверен и пуст».
- `WebFetch` на `youtube.com/watch?v=...` подтверждённо возвращает только страничный хром (SPA-оболочка), без текста — это ожидаемое, задокументированное ограничение инструмента, не новость.
- Telegram — подтверждено: ни MCP, ни разрешённого домена `t.me`. Не обходился. Один раз наткнулась на публичную статью на vc.ru (обычный веб-поиск, не заход в сам канал) с разбором реального Telegram-пайплайна — использована как обычный источник, это разрешено брифом отдельной строкой.

---

## 1. Мультиагентные фреймворки — CrewAI, AutoGen, LangGraph

### CrewAI — Confirmed, primary (официальный docs + официальный пример-репозиторий, прочитаны напрямую)

**Framework-level контракт** (`docs.crewai.com/en/concepts/agents`): агент = `role`/`goal`/`backstory` (обязательные поля) + опциональный булев `memory` («enable the agent to maintain context across multiple interactions») + опциональный булев `allow_delegation` («permits the agent to delegate tasks to other agents») + выбор `Process.sequential` / `Process.hierarchical` на уровне всего crew. То есть framework сам по себе **предоставляет** примитивы памяти и делегирования — это не отсутствует в CrewAI как таковом.

**Официальный пример именно маркетингового кейса** — `crewAIInc/crewAI-examples`, `crews/marketing_strategy/src/marketing_posts/{config/agents.yaml, crew.py}`, прочитан целиком напрямую (raw GitHub):
- 4 роли: `Lead Market Analyst`, `Chief Marketing Strategist`, `Creative Content Creator`, `Chief Creative Director` — только `role`/`goal`/`backstory`, без единого слова о границах ответственности (что агент НЕ делает).
- В `crew.py` **прямо прописано** `memory=False` на каждом агенте — память программно выключена, не просто не задокументирована.
- `process=Process.sequential` — иерархический процесс (тот самый, где реально работал бы `allow_delegation`) закомментирован в исходнике как альтернатива, не используется.
- Делегирование: `allow_delegation` нигде не установлен → по умолчанию `False`.

**Прямой вывод**: framework даёт строительные блоки (`memory`, `allow_delegation`, hierarchical process), но **официальный, эталонный маркетинговый пример CrewAI осознанно ни один из них не использует** — это чистая линейная функция-конвейер (роль → задача → следующая роль), не агент с состоянием и границами.

### AutoGen — Confirmed, primary (реальный код community-примера прочитан целиком)

Официальной, framework-level маркетинговой персоны в AutoGen нет (в отличие от CrewAI, у которого есть официальный example-репозиторий). Найденный, повторяемый в нескольких независимых вторичных источниках пример — «**Agentcy**» (агентство-паттерн: Manager/Researcher/Strategist/Copywriter/Marketer/Media Planner). Первичный код найден и прочитан напрямую: `witchcraftery/brainswarm/main.py`.

**Механизм**: каждый агент — `AssistantAgent` с полем `system_message` (роль как текст-инструкция, включая явную команду завершать реплику словом `"TERMINATE"`) и опциональным `function_map` (доступ к 2 общим tool-функциям — `research`/`write_content`, не к другим агентам напрямую). Координация — `GroupChat`/`GroupChatManager`: агенты говорят по очереди в общем чате, «делегирование» — это не вызов инструмента с явным контрактом входа/выхода (как в OpenExecutive ниже), а мягкая инструкция в тексте промпта «Coordinate closely with the Agency_Manager». Памяти между сессиями нет вообще — `GroupChat` эфемерен, состояние живёт только внутри одного запуска.

**Прямой вывод**: слабее CrewAI-примера по структуре (нет даже разделения ролей на конфиг-файл), делегирование и границы существуют только как естественный язык внутри `system_message`, не как проверяемый контракт.

### LangGraph — проверено, прямого совпадения не найдено (честно, не подогнано)

Единственный найденный «маркетинговый агент» на LangGraph с полным техническим разбором (`swmansion.com`, блог-серия, прочитан напрямую) — это **не персона маркетингового стратега**, а система из 3 агентов (`Search`/`Critic`/`Selector`), которая ищет и ранжирует веб-сайты — потенциальные маркетинговые возможности/партнёрства. Название совпадает с темой брифа, содержание — нет: там нет ни ролевой персоны CMO/стратега, ни границ ответственности, ни памяти, ни делегирования в смысле брифа. Зафиксировано как **честный промах**, не притянуто в таблицу.

**Насыщение по п.1**: два независимых фреймворка (CrewAI, AutoGen) сошлись на одном и том же паттерне — framework может больше, чем используют в реальных маркетинговых примерах; ни один не даёт готовой, документированной персоны маркетингового стратега с границами+памятью+делегированием «из коробки». Третий фреймворк того же типа (LangGraph) это не опроверг, а только подтвердил через явный промах — поиск остановлен здесь по факту исчерпания, не по нехватке времени.

---

## 2. Опубликованные AI CMO / marketing-agent персоны — главная находка захода

### AICMO Marketing Prompt Collection — Confirmed, primary (репозиторий прочитан напрямую)

`AICMO/AiCMO-Marketing-Prompt-Collection` — организован по функциям отдела маркетинга (`01_CMO-&-Leadership` … `07_Hybrid-AI-Team-Management`). Специально проверен самый релевантный вопросу брифа раздел — `07_Hybrid-AI-Team-Management/Multi-Agent-Orchestration/...Handoff-Protocol-Design...md`, прочитан целиком: даже здесь, где заголовок обещает «протокол хендоффа между агентами», реальное содержимое — это **промпт**, который просит LLM *сгенерировать* документ-протокол с плейсхолдерами (`[Agent A]`, `[Company Name]`) — не реализованный, работающий механизм, а шаблон для того, чтобы пользователь сам спроектировал такой механизм у себя. Уровень детализации коллекции в целом — операционные промпты без встроенной памяти/делегирования, что подтверждает предыдущий вывод по этому источнику (не проверялось повторно вслепую — перепроверен конкретно самый сильный на вид файл).

### agency-agents (`msitarzewski/agency-agents`) — Confirmed, primary (репозиторий и файл прочитаны напрямую)

Библиотека Claude Code субагентов, ~200+ файлов по категориям, включая `marketing/` (35 файлов: `marketing-growth-hacker.md`, `marketing-seo-specialist.md`, `marketing-social-media-strategist.md`, канало-специфичные — TikTok/Xiaohongshu/Douyin/LinkedIn и т.д.). Прочитан целиком пример `marketing-growth-hacker.md`: формат — `name`/`description`/`tools`/`color`/`emoji` во frontmatter + Identity/Capabilities/Decision Framework/Success Metrics в теле. Это тактические, канало- или функция-специфичные персоны (Growth Hacker, не Marketing Strategist целиком), без явного поля границ («вне scope»), без явного механизма памяти (полагается на память host-раннера, не описана в файле), без явного делегирования другим агентам (только список `tools`).

### **`SenteLabsAI/OpenExecutive` — Confirmed, primary, прочитан на уровне исходного кода — единственная найденная система с реальным совпадением роль + границы + память + делегирование**

Открытый (лицензия «Other», активный: **1712 звёзд, последний push вчера** относительно сегодняшней даты 2026-08-28), production-ориентированный проект: «AI-powered virtual executive team — a single coherent executive persona backed by 8 specialist Claude agents». Среди 8 специалистов — **CMO** (`packages/core/openexecutive/agents/marketing.py` → `MarketingAgent(name="cmo", domain="marketing")`). Все 4 компонента ниже подтверждены прямым чтением реального исходного кода, не пересказом README.

**Роль** — `prompts/domain_prompts.py`, `CMO_PROMPT` (полностью прочитан): «You are the Chief Marketing Officer — a specialist in go-to-market strategy, brand building, and communications» + 5 доменов экспертизы (GTM, Brand strategy, Demand gen, Communications, Customer marketing) + именованные, конкретные правила-эвристики («Positioning before tactics (April Dunford)», «Brand vs. demand: don't fund one by starving the other», «One ICP at a time») + фиксированная 4-шаговая процедура рассуждения + инструкция вплетать `<failure_cases>` в ответ, если они переданы в контексте.

**Границы (не проза, структурированное поле)** — `departments/models.py`, `DepartmentCharter(mission: str, scope: list[str], out_of_scope: list[str])`. Реальный, не абстрактный пример из фикстуры компании (`fixtures/companies/halcyon_motors/departments.yaml`, отдел `marketing`):
```
scope: [Brand/positioning/TCO-narrative, Demand generation and funnel,
        Competitive messaging response, Channel/creative spend within budget]
out_of_scope: [Pricing decisions (owned by Strategy/Finance),
               Product roadmap commitments]
```
Ровно то структурное разделение «что входит / что явно не входит», которого не хватало в старом `marketing_agent.md` (см. закрытую находку от 2026-08-27, Карточка 2) — здесь оно есть, работает и подгружается в промпт исполнителя.

**Память — два независимых, разного назначения слоя**, оба реально инжектируются в system/user-контекст (`agents/base.py`, метод `analyze()`):
1. `episodic_context` — «past_decisions», структурированный SQLite-журнал прошлых решений/рекомендаций, извлекается фоновым проходом на `claude-haiku-4-5` после каждого ответа.
2. `department_memory` — институциональная память **уровня отдела**, не отдельного агента: через Honcho (векторная память), синтезированный «голос коллеги по отделу», подгружается по `department_slug` через `orchestrator/router.py → _prefetch_department_for_call`. Порядок в промпте намеренно фиксирован (`department_memory` оборачивает `past_decisions`) ради стабильности кэша.

**Делегирование — два разных, не смешанных механизма**:
1. Исполнение по запросу: оркестратор (Executive) вызывает `consult_specialist` tool с параллельным fan-out (`orchestrator/router.py`, `route_parallel`, capped через `resolve_fanout_cap`) — CMO получает `query`+`context`, ничего больше не видит.
2. Автономное проактивное действие отдела: `departments/authority.py`, `gate_action()` — трёхуровневый `authority_level` (`auto_execute` / `propose_only` / `escalate`) решает, выполняется ли инициатива отдела сразу, уходит на согласование конкретному approver'у, или эскалируется принципалу. В фикстурах у `marketing` — `propose_only` (не исполняет сама, предлагает).

**Честная оговорка, важная для точного ответа на вопрос брифа**: CMO в этой архитектуре — **цель** делегирования (её вызывает оркестратор), не сама делегирующая сторона; «единая когерентная персона», под которой продукт себя продаёт — это Executive-фасад, а не сам CMO как самостоятельный, отдельно продаваемый «AI Marketing Strategist». То есть это не прямое совпадение «независимый AI Marketing Strategist продукт = роль+границы+память+делегирование», а совпадение **внутри** коherentной executive-команды, где у *специалиста*-CMO эти четыре элемента есть все одновременно, в реальном работающем коде.

---

## 3. YouTube — технически заблокировано, но один реальный кейс найден через связанную страницу

Прямой транскрипт получить не удалось (см. «Технический доступ» выше). Реальный, релевантный видео-эпизод найден: **AI x GTM Talks, эп. 60** (`youtube.com/watch?v=TxF08ewBH54`), Chintu Parikh (VP/CMO-уровень маркетинга, nOps) — построили внутреннего AI-маркетинг-агента «Gen». Контент получен не из транскрипта самого видео, а из содержательного пересказа на официальной companion-странице эпизода (`kwanzoo.com`, прочитана напрямую) — это вторичный, не дословный источник, помечаю честно, не выдаю за то же качество, что прямое чтение.

**Механизм (по пересказу)**: команда перешла от «сотен ручных промптов в неделю» к нулю — Gen работает по cron-расписанию, сам готовит брифинги/блог-посты/LinkedIn-посты по утрам. «16 Python-based skills» описаны как функциональный эквивалент «16 специалистов команды» (каждый — отдельная узкая задача: конкурентный мониторинг, SEO-копирайтинг, CEO-брифинг, LinkedIn-контент), не как персоны с собственным системным промптом каждая — то есть это скорее декомпозиция на **функции/инструменты**, чем на **агентов-персон** в смысле брифа. Изолированная инфраструктура — отдельный AWS VPC. О явной структуре границ/памяти/делегирования между этими 16 skills пересказ ничего не говорит — не проверено, честно не додумано.

## 4. Telegram — один реальный, но неполный по объёму скоупа пример (первичный, найден обычным веб-поиском)

vc.ru, «Семь AI-агентов ведут мой Telegram-канал» (первоисточник, first-person практика, прочитана целиком напрямую) — реальный, именованный пайплайн из 7 ролей: **Стратег** (Марк, раз в 2 недели, документ-приоритеты) → Исследователь (Игорь) → Контент-планировщик (Стас) → Копирайтер (Кирилл) → Редактор (Рита, проверка по списку из 420 запрещённых фраз — конкретный, работающий rule-based гейт границ) → Аналитик (Аня) → Комьюнити (Марина). Оркестрация — `launchd` (cron-планировщик macOS), не агентный tool-call; каждый агент запускается по расписанию как отдельный процесс, не хранит явную межсессионную память (кроме документов-артефактов, которые следующий агент читает как вход). Скоуп — контент-операции Telegram-канала (редакционный, не полный GTM/brand-стратег), поэтому это **не** прямое совпадение с ролью AI Marketing Strategist из брифа, а близкий, честно неполный кейс: роль «Стратег» здесь узкая (приоритеты контента на 2 недели), не позиционирование/бренд/GTM целиком.

---

## Прямой ответ на вопрос брифа

**Вывод v1/v2 «полное совпадение роль+границы+память+делегирование нигде не найдено» — опровергнут, с точным указанием, где именно.**

Найден один реальный, primary, активно поддерживаемый (1712⭐, коммиты вчера) проект — `SenteLabsAI/OpenExecutive` — где у CMO-специалиста внутри executive-команды одновременно, в реально работающем коде, есть: именованная роль с конкретными эвристиками, **структурированные** (не прозаические) границы `scope`/`out_of_scope`, два раздельных слоя памяти (per-turn эпизодическая + per-department институциональная), и рабочий механизм делегирования (tool-call fan-out + authority-gated автономность). Это не общий паттерн индустрии (единственный найденный случай такой полноты после проверки 2 фреймворков, 2 промпт-коллекций, 1 YouTube-кейса, 1 Telegram-кейса) — но и не «нигде», как звучал прежний вывод. Оговорка: совпадение — внутри коherentной команды, где делегируют CMO, а не CMO делегирует сам; как самостоятельный, отдельно продаваемый «AI Marketing Strategist»-продукт такого полного совпадения не найдено нигде, включая OpenExecutive.

Остальные источники (фреймворки, промпт-коллекции, YouTube-кейс, Telegram-кейс) подтверждают исходный вывод, а не опровергают его — они системно тоньше: либо только роль-текст без структурных границ/памяти/делегирования (AICMO, agency-agents, AutoGen-Agentcy), либо структура есть, но осознанно не используется (CrewAI официальный пример), либо не про ту роль вовсе (LangGraph-пример).

---

## Переиспользуемые находки (Definition of Done 5a)

**Карточка 1 — `DepartmentCharter` (scope/out_of_scope как структурированные поля, не проза) — реальный, работающий образец решения именно того пробела, который нашла AMA/CIM-сверка 2026-08-27**

- **Назначение**: Pydantic-модель из `OpenExecutive/departments/models.py` — `mission: str`, `scope: list[str]`, `out_of_scope: list[str]` как отдельные, машиночитаемые поля роли, а не абзац текста.
- **Почему интересен**: предыдущая находка (2026-08-27, Карточка 2) назвала проблему — у `AI Marketing Strategist/HOME.md` «вне scope» названо только частично (Content/Distribution/Measurement), Pricing/Product/Data-инфраструктура — немые пробелы. Здесь — реальный, работающий пример системы, где это структурное поле есть у каждого отдела с самого начала, не как заплатка.
- **Наш вывод**: адаптировать.
- **Если адаптируем — с чем**: с существующей структурой `AI Marketing Strategist/HOME.md` — не менять содержание границ (это отдельное, gated решение владельца/Engineer), а рассмотреть сам формат представления «вне scope» как структурированного, а не только прозаического списка.
- **Куда идёт**: Foundation (формат ролевых файлов) / конкретно `AI Marketing Strategist`, если владелец решит это внедрять — не решается этим отчётом.
- **Приоритет**: средний — это про форму, не про содержание; ценно только если владелец уже планирует ревизию границ этого агента.
- **Почему именно так**: разница между «есть строка вне scope» и «структурное поле, которое парсится/проверяется» — конкретна и проверяема на этом реальном примере, не абстрактная рекомендация.
- **Что конкретно переносим**: ✔ сам факт, что такой структурированный формат существует и реально работает в production-проекте; ✘ конкретные названия полей/технологию (Pydantic/Python) — irrelevant для markdown-родного Foundation.

**Карточка 2 — Двухслойная память (per-turn episodic + per-department institutional) как отдельные, не смешанные механизмы**

- **Назначение**: в OpenExecutive память специалиста — это не один блок, а два: `episodic_context` (журнал решений, SQLite, извлекается фоновым проходом) и `department_memory` (институциональная память отдела через Honcho, «голос коллеги», не привязана к конкретному агенту-исполнителю).
- **Почему интересен**: даёт конкретный, работающий словарь для разговора о памяти AI-агентов вообще — «решение, которое агент помнит, что сам порекомендовал» (episodic) — это не то же самое, что «то, что знает отдел как институция» (department/institutional) — то же различие всплывало в прежних батчах (`Batch AGT`, буквы E/F/G/H в `MECHANISM_LIBRARY.md`), здесь — свежий, конкретный, современный (2026-08) экземпляр этого различия на реальном коде.
- **Наш вывод**: отложить — это facts-for-later, не сразу применимая рекомендация конкретному агенту.
- **Если адаптируем — с чем**: с `instructions/memory-rules.md` (три уровня Foundation-памяти) — при следующем пересмотре стоит явно сверить, соответствует ли текущая трёхуровневая модель (MEMORY.md/memory-дневник/knowledge) этому эпизодическое-vs-институциональное разделению, или это разные оси.
- **Куда идёт**: Foundation, `instructions/memory-rules.md` — не решается этим отчётом.
- **Приоритет**: низкий — интересный концептуальный материал, не срочная проблема.
- **Почему именно так**: смешивать «что я лично решил» и «что отдел знает как институция» в одном поле — конкретная архитектурная ошибка, которую этот проект явно избегает (комментарий в коде: «Order is intentional — past_decisions stays closest to the query for cache stability... department_memory wraps it») — стоит знать до того, как понадобится проектировать что-то подобное самим.
- **Что конкретно переносим**: ✔ само различение двух типов памяти и то, что их можно/нужно инжектировать раздельно; ✘ конкретную технологию (Honcho, SQLite) — это реализационная деталь чужого стека.

**Карточка 3 — `authority_level` (auto_execute / propose_only / escalate) как отдельный, не совпадающий с «делегированием задачи» механизм автономности**

- **Назначение**: в OpenExecutive это два разных вопроса про один и тот же отдел: (а) «кому маршрутизируется конкретный запрос» (routing/tool-call делегирование) и (б) «может ли этот отдел сам, без спроса, довести до конца собственную проактивную инициативу» (authority gate). Marketing в фикстурах — `propose_only`: сама не исполняет, предлагает.
- **Почему интересен**: вопрос брифа звучал как единое «делегирование», но реальная система различает эти два измерения так же, как прошлая находка (2026-08-27, Карточка 1) различала «содержание vs вовлечённость» — снова та же форма ошибки («смешать две независимые оси»), но в другой части профессии (не человеческая карьерная лестница, а инженерный механизм автономности агента).
- **Наш вывод**: отложить.
- **Куда идёт**: Foundation / любой будущий агент, для которого встанет вопрос «может ли он действовать сам, не только отвечать на запрос» — не относится напрямую к `AI Marketing Strategist` (у него сейчас нет проактивных cron-действий).
- **Приоритет**: низкий — пока ни один агент дома не имеет проактивных cron-инициатив, вопрос не горит.
- **Почему именно так**: то же самое расслоение осей, что уже подтверждало свою полезность 2026-08-27 — стоит зафиксировать как повторяющийся, не разовый паттерн.
- **Что конкретно переносим**: ✔ сам факт разделения «кому маршрутизируется» vs «может ли действовать без спроса» на два независимых механизма; ✘ конкретные три уровня (auto_execute/propose_only/escalate) как готовое решение — не проверено, подходит ли этот набор уровней для контекста Foundation.

---

## Источники

- `docs.crewai.com/en/concepts/agents` — официальная документация CrewAI (framework-level контракт агента), прочитана напрямую.
- `github.com/crewAIInc/crewAI-examples`, `crews/marketing_strategy/src/marketing_posts/config/agents.yaml` + `crew.py` — официальный маркетинговый пример CrewAI, прочитан целиком напрямую (raw).
- `github.com/witchcraftery/brainswarm`, `main.py` — реальный код AutoGen-based «Agentcy»-паттерна, прочитан целиком напрямую (raw).
- `swmansion.com/blog/building-agents-with-langgraph-part-4-4-...` — официальный технический блог, LangGraph-based «marketing agent» (по факту — поиск возможностей, не CMO-персона), прочитан напрямую.
- `github.com/AICMO/AiCMO-Marketing-Prompt-Collection`, файл `07_Hybrid-AI-Team-Management/Multi-Agent-Orchestration/...Handoff-Protocol-Design...md` — прочитан целиком напрямую (raw).
- `github.com/msitarzewski/agency-agents`, `marketing/marketing-growth-hacker.md` + дерево `marketing/` (35 файлов) — прочитаны напрямую (raw + API tree listing).
- **`github.com/SenteLabsAI/OpenExecutive`** — основной первичный источник этого захода, прочитан на уровне исходного кода напрямую (raw): `agents/marketing.py`, `agents/base.py`, `prompts/domain_prompts.py` (весь `CMO_PROMPT`), `orchestrator/router.py`, `departments/models.py`, `departments/authority.py`, `departments/registry.py`, фикстуры `fixtures/companies/halcyon_motors/departments.yaml` и `fixtures/companies/tandem_robotics/departments.yaml` (обе — для кросс-проверки, что структура границ не одноразовая, а системная — она повторяется во второй, независимой компании-фикстуре с другим доменом бизнеса). Метаданные репозитория (звёзды/активность) — через `api.github.com`.
- `kwanzoo.com/video/ai-x-gtm-talks-episode-60-ai-agents-for-marketing` — companion-страница реального YouTube-эпизода (Chintu Parikh, nOps), прочитана напрямую; сам транскрипт видео `youtube.com/watch?v=TxF08ewBH54` — не получен (см. «Технический доступ»).
- `vc.ru/ai/2761843-kak-sem-ai-agentov-vedut-telegram-kanal` — first-person практика, реальный 7-агентный Telegram-пайплайн, прочитана целиком напрямую.

## Внутренний контекст, использованный без повторной проверки

- Коммит `5e22953` (ветка `worktree-marketing-strategist-benchmark`) — эталон AMA/CIM, прочитан целиком, не повторён по прямому указанию брифа.
- `knowledge/EVIDENCE_INDEX.md`, `knowledge/MY_AGENTS.md` — проверены на предмет уже закрытых находок по теме, прямых совпадений не найдено.
- Ветки `worktree-marketing-persona-search`, `worktree-marketing-agent-persona-search` — проверены через `git log --oneline`, подтверждено отсутствие уникальных коммитов.
