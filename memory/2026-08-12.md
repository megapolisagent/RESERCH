# Дневник 2026-08-12

## Engineering-ревизия: приведение к единой Foundation-системе

### Что сделали

- Engineer провёл инженерную ревизию (не оценку методологии) и достроил отсутствовавшую Foundation-структуру: `VISION.md`, `HOME.md` (роль делегирует в `MISSION.md`, не дублирует), `AGENTS.md`, `SOUL.md`/`PROFILE.md` (честно пустые шаблоны, не выдуманы), `ROUTING.md` (с реальными строками роутинга по существующим `knowledge/`-файлам), `UPDATE.md`/`update.sh` (сразу в исправленном виде — перенесено исправление, найденное на Engineer 2026-08-12, оригинал стирал специализацию при обновлении), `DECISIONS.md` (формат + один Proposal про отношение к `REVIEW_LOOP.md`, без переноса существующих решений), `OPEN_QUESTIONS.md`, `COMMUNICATION.md`, `instructions/memory-rules.md`, `workspace/`.
- `skills/reverse-engineering/` упакован в формат `SKILL.md` + `references/` (методология перенесена без единой правки, сверено контрольными суммами), зарегистрирован в Skill System (`origin: own`, категория `RESEARCH`, статус `DRAFT`).
- `.claude/settings.local.json` и `.claude/settings.json` — убраны мёртвые одноразовые записи (в т.ч. следы сборки самого Engineer), оставлены только повторно используемые разрешения.
- `.mcp.json` НЕ тронут — расхождение с реально используемыми `mcp__firecrawl__*`/`mcp__exa__*` найдено, но не исправлено: нет проверенных данных для правильной записи серверов, править вслепую значило бы выдумывать конфигурацию.

### Что осталось нетронутым осознанно

- Содержание `MISSION.md`, `PRINCIPLES.md`/`METHODOLOGY.md`/`REVIEW_LOOP.md`, `knowledge/*` — Craft, не предмет этой ревизии.
- Существующие инлайн-решения `REVIEW_LOOP.md` — не перенесены в `DECISIONS.md`, вопрос оставлен как `Proposal`.

### Решения

- Engineering ≠ Craft — граница проведена явно, зафиксирована в `DECISIONS.md` этого дома и в отчёте Engineer (`ENGINEER/workspace/2026-08-12-engineering-revision-report.md`).

## Первый реальный вызов Engineer → Researcher напрямую

Engineer вызвал Researcher прямо (без владельца в цепочке) — первый реальный прогон только что подтверждённого физического канала. Бриф: как реально устроено прямое взаимодействие orchestrator→specialist агентов (вызов/результат/уточнение/повторный вызов) в системах, которые уже так работают, до того как закреплять собственный механизм.

Нашлись две структурно разные, не конвергентные семьи: call-and-return (Claude Code subagents/resume, LangGraph supervisor+`Command`) и baton-pass без возврата (OpenAI Agents SDK handoffs — подтверждённый негативный факт, не пробел). У Anthropic в её собственной multi-agent research system достаточность результата оценивается неявным LLM-суждением, а уточнение — это новый subagent, а не resume старого; это прямо противоречит и Claude Code (resume сохраняет полную историю специально) и уже известному metaswarm (Capability 5 — reviewer обязательно свежий). Противоречие зафиксировано как есть, не сглажено.

Для связки Engineer↔Researcher ближайший реальный аналог — не subagent-resume (это внутри одной сессии), а cross-session messaging: только текст, без истории и файлов, с обратным адресом. Само решение, каким должен быть механизм уточнения у нас, этим исследованием не принято — осталось Research, не Design, по своему же правилу.

Полный ответ: раздел `knowledge/CAPABILITY_MAP.md` → «Refinement — Orchestrator↔Specialist Invocation Contract»; сырое evidence — `knowledge/evidence/2026-08-12-orchestrator-specialist-invocation.md`; вопрос закрыт в `OPEN_QUESTIONS.md`.

## Self-audit: собственная методология против внешней практики research/intelligence-агентов

Второй прямой вызов Engineer → Researcher в тот же день. Бриф: применить `reverse-engineering` к себе — сверить пятистадийный процесс, иерархию источников/Definition of Done, evidence-loop/Review, контракт вывода — против внешнего reference set (реальные AI research-agent системы + реальная human-intelligence tradecraft + OSINT/fact-checking + benchmarking), не против собственных документов.

Итог по элементам, не одним усреднённым вердиктом:
- Принципы (P0–P6) — держатся хорошо, каждый на установленной внешней дисциплине.
- Форма пятистадийного пайплайна — сходится и с классическим Intelligence Cycle, и с 4-стадийными пайплайнами deep-research-agent обзоров 2025–2026 на уровне «собрать → вскрыть механизм → судить», но не буквально как 5 стадий у обеих семей.
- Verification как отдельная стадия с градацией источников (5 уровней) — подтверждена human-intelligence/OSINT tradecraft (ICD 203, NATO/Admiralty Code, Bellingcat), **не** подтверждена как текущая практика AI research-agent инженерии: свежая проверка собственного production-поста Anthropic про их multi-agent research system прямым вопросом показала, что у них нет формальной иерархии источников и confidence-тиров — только неформальная эвристика в промпте. Это честно зафиксировано как расхождение, не как провал: наша конструкция здесь строже отраслевого AI-agent дефолта, опора просто из другого класса источников.
- Evidence-loop/Review Loop — подтверждён вдвойне: не только классическими SRE/Argyris&Schön/OODA, но и напрямую тем, как сам Anthropic итеративно строил свою реальную research-систему (LLM-judge рубрика, человеческая проверка ловит то, что автоматика упускает, rainbow deployment).
- Контракт вывода (`MISSION.md`) — «intelligence, не рекомендация» и «разногласие не сглаживается» оба подтверждены реальной практикой intelligence community (разделение анализа от policy, сохранение dissent в NIE через footnotes/основной текст, не голосование к консенсусу).

Одна реальная находка изменила сам методологический документ, не только этот дневник: Definition of Done, условия 4/5 («что переносится / что явно нет») были помечены как `original` — проверка нашла, что это уже существующая дисциплина cross-organizational benchmarking (Xerox, 1979, принцип «adapt, not adopt»). Исправлено инлайн в `skills/reverse-engineering/references/METHODOLOGY.md` по собственному правилу «agent may revise v0.x between Evidence Reviews», не оставлено только заметкой в knowledge.

Само это исследование — ещё один подтверждающий случай собственного честного ограничения `REVIEW_LOOP.md`: double-loop сработал снова только потому, что его запросила внешняя сторона (Engineer, по вопросу от владельца), не потому что процесс заметил разрыв сам.

Полный ответ: `knowledge/CAPABILITY_MAP.md` → «External Validation» (2026-08-12); сырой reference-set — `knowledge/EVIDENCE_INDEX.md`, Batch SELF; исправление — `skills/reverse-engineering/references/METHODOLOGY.md`; портфельная запись — `knowledge/MY_AGENTS.md` → «Researcher (this project)». Вопрос **не** зарегистрирован в собственном `OPEN_QUESTIONS.md` этого дома (только у Engineer) — там и остаётся закрывать.

## Третий прямой вызов Engineer → Researcher: реальные механизмы автоматического выбора Skills/tools/моделей и оптимизации стоимости

Отдельная (не третья часть предыдущих двух) задача от Engineer в тот же день: реальная production-практика 4-слойной «Capability System» (Skills / Tools-MCP-Plugins / Models / Routing) — по мотивам провала с Exa (инструмент физически подключён, но правило запрещало его *рассматривать*, поэтому он никогда не использовался). Восемь под-вопросов, критерий зафиксирован до поиска. В ходе сессии координатор дважды присылал уточнения по реальным, не гипотетическим триггерам: (а) собственная сессия Engineer заметно тяжелеет по токенам при параллельных фоновых агентах — усилить пункт 5 про fan-out; (б) явное разделение «современный и подобранный под профессию набор» ≠ «максимум установлено» — усилить пункт 7 про периодическое (не разовое) списание capabilities.

Нашлось 3+ реальных production-механизма, разобранных на уровне устройства: Anthropic tool search tool (BM25/regex + `defer_loading`, >85% экономии токенов, официально названный порог деградации точности выбора инструмента после ~30-50 штук), RouteLLM/vLLM Semantic Router (калиброванный порог/BERT-классификатор, 85% экономии при 95% качества), GPT-5 router (единственный найденный по-настоящему автоматический выбор модели под сообщение — но только внутри одного вендора). Два честных «не найдено»: автоматический межвендорный (Claude/Gemini/Codex) роутинг модели под подзадачу в проде нигде не найден — только ручные статические пайплайны; периодическая переоценка/списание уже установленных capabilities по расписанию тоже не найдена нигде — только вручную/по симптому вызываемые, но реальные и работающие инструменты (`mcp-audit`, `deadskills`, `skillreaper` — KEEP/DISABLE/REVIEW или active/zombie/dead, обязательное подтверждение человека, у части — обратимый карантин). По усиленному fan-out-вопросу — количественный ответ: ролевое разделение по стоимости (дорогой оркестратор/дешёвые исполнители — официальная рекомендация Anthropic + бенчмарк Arize, 96% качества при 46% стоимости) рядом с честно названным контр-фактом (Systima «The Subagent Tax»: параллельный fan-out стоил 2.6-5.9x токенов и не был быстрее ни разу в бенчмарке).

Зафиксировано прямое противоречие, не сглажено: практика авто-обновления Skills без ревью человека (`npx skills update` из `main` при каждом старте сессии) существует рядом с прямо противоположной рекомендацией «после смены модели вручную перепроверяй все skills» — ни один источник их не примиряет.

Отдельная честная находка про сам процесс: координатор сослался на Skill `youtube-analysis` как «в Skill System, VERIFIED·LIVE» — проверено напрямую против реального списка доступных скиллов этой сессии и таблицы `HOME.md`, такого скилла в рабочем составе нет. Не выдумано использование — скилл не вызывался, использован WebSearch/WebFetch по YouTube как замена (видео не дало транскрипта — отмечено как «not directly accessed» по правилу `METHODOLOGY.md`). Это — маленький, но реальный случай ровно того явления, которое исследовалось: «сказано, что доступно» ≠ «действительно доступно агенту прямо сейчас».

Полный ответ: `knowledge/CAPABILITY_MAP.md` → «Refinement — Automatic Capability/Model Selection: Skills, Tools/MCP, Models, Cost»; сырое evidence — `knowledge/evidence/2026-08-12-capability-model-routing-selection.md`; реестр источников — `knowledge/EVIDENCE_INDEX.md`, Batch ROUTE; вопрос закрыт в `OPEN_QUESTIONS.md`.

### Что осталось
- Наложение найденного на реальную архитектуру Engineer/Skill System сознательно не сделано — вне роли этой сессии (Responsibility Boundary Gate), это следующий шаг Engineer.
- Видео-источник (YouTube) не дал содержательного материала напрямую — этот пробел честно назван, не выдан за покрытый.

### Решения
- Второй раз подряд в этот день: внешний вопрос от Engineer закрывается по практике `OPEN_QUESTIONS.md`, без попытки спроектировать решение для принимающей стороны.

## Четвёртый прямой вызов: аудит capability-map Engineer (17 Skills категории ENGINEERING)

Бриф от Engineer: не принимать «17 Skills = собственные способности Engineer» как факт — переоценить саму декомпозицию (MERGE/SPLIT), построить capability map, сверить с внешней практикой, вынести вердикт по каждой, назвать MISSING capabilities (в т.ч. подозреваемый пробел владельца — независимая оценка результата уже собранного агента), ответить на вопрос про Locked-роль.

Первая и главная находка изменила саму рамку задачи: прямая проверка (`ls ENGINEER/skills/` против `registry.json`) показала — из 17 Skills категории ENGINEERING в общем Skill System реально установлены на Engineer только 3 (`repository-design`, `decision-documentation`, `tool-selection`). Остальные 14 — записи каталога, ни разу не подтянутые через `installer`. Это не противоречит уже известному (Engineer сам это зафиксировал в `DECISIONS.md` 2026-08-12), но формулировка задачи трактовала весь список как «способности Engineer» буквально — исправлено явно, не молча.

Декомпозиция пересобрана: два MERGE-кандидата (dispatching-parallel-agents+executing-plans+subagent-driven-development — один execution-orchestration с тремя режимами; skill-creator+writing-skills — одна capability «авторство и тестирование Skill», независимо переизобретённая из двух разных источников — anthropics/skills и obra/superpowers), два SPLIT-кандидата (claude-api разделён на platform-reference и model-migration/maintenance; model-tiering-решение вынесено из subagent-driven-development как переиспользуемая способность). Найдена и проверена находка отдельно от брифа: `architecture-review` и `capability-creation-methodology` буквально ссылаются на файлы другого агента (`idea-calibration.md`, Bootstrap Sequence → `PROJECT_DNA.md`) — прямой поиск по ENGINEER дал ноль совпадений, значит оба Skill при установке «как есть» сломаны, не просто неоптимальны — REPLACE, не KEEP.

MISSING capabilities: подтверждён названный владельцем пробел (независимая пост-сборочная оценка результата) — и подтверждено дополнительно (свежий прямой fetch `anthropic.com/engineering/demystifying-evals-for-ai-agents`), что даже текущая официальная практика Anthropic не решает эту независимость сама — фокус на калибровке LLM-judge и распределённом авторстве evals, не на организационном разделении «строит vs. оценивает». Ещё три пробела: maintenance/drift-ревалидация после смены модели (нигде в мире не найден плановый механизм, только ручной/по симптому — уже известно из сегодняшнего же более раннего исследования); полноценный discovery Skills+MCP+Models (не только Skills) — прямое противоречие с сегодняшним же Locked «Эталоном обязанностей Engineer» пункт 1; permissions/least-privilege-аудит — не новая находка, переподтверждение уже зафиксированного самим Engineer наблюдения.

Ответ на главный вопрос: Engineer пока не может полностью и устойчиво выполнять свою Locked-роль этим набором — три конкретных, доказанных (не гипотетических) разрыва, каждый уже проявился в реальной работе, залогированной в `ENGINEER/DECISIONS.md` (Skill-авторство трижды делалось импровизацией без единой установленной Skill; единственная реальная оркестрация Engineer↔Researcher прошла без установленной методологии и её же правило до сих пор `Proposal`; единственная реальная правка кода — `update.sh` — проверена импровизированным тестом, не установленной дисциплиной), плюс три полностью отсутствующие способности выше.

Полный ответ: `knowledge/evidence/2026-08-12-engineer-capability-map-audit.md`; портфельная запись — `knowledge/MY_AGENTS.md` → «Engineering Agent», третий cross-reference; источники — `knowledge/EVIDENCE_INDEX.md`, Batch ENGCAP.

## Пятый прямой вызов: observability/execution record для самостоятельно живущих агентов

Отдельная новая исследовательская задача, вопрос зарегистрирован в `ENGINEER/OPEN_QUESTIONS.md` (2026-08-12). Найден повод: Engineer вручную вытащила execution trace дочернего суб-агента через Bash+Python по JSONL — сработало, но это частный случай (subagent внутри одной сессии). Реальный вопрос шире: как это устроено для агентов с самостоятельной жизнью, не дочерних вызовов.

Разобраны 4 системы на уровне механизма (прямой fetch официальной документации для двух из них): Anthropic Managed Agents (Sessions API — платформа хранит, `GET /v1/sessions/{id}/events`, история без TTL, sandbox — 30 дней, полностью программный доступ, явно работает для независимо живущего агента); OpenAI Responses+Conversations API (важная находка «на злобу дня»: Assistants/Threads API, названный в брифе как вероятный ближайший аналог, снимается с эксплуатации 2026-08-26 — через две недели от сегодня — актуальна уже его замена); OpenTelemetry GenAI semantic conventions + LangSmith (структурная находка: OTel сам не хранит ничего, только формат данных — хранение и retrieval-by-id всегда у отдельного backend, LangSmith — конкретный пример такого backend, 14/400 дней retention); и — самое прямое попадание в реальный кейс — собственный нативный механизм Claude Code (`code.claude.com/docs/en/sessions`, прямой fetch): `claude -p --resume <session-id> --output-format json` реально работает **не только для дочерних субагентов**, а для любой независимо запущенной сессии на той же машине — прямая цитата из документации подтверждает поиск ID «across every other project on this machine».

Применимость к реальному кейсу (Claude Code, раздельные репозитории Engineer/Researcher, без общего сервера) — сказано прямо, не спроектировано: три из четырёх систем требуют инфраструктуры, которой нет (миграция на другую платформу или отдельный collector/сервис); нативный механизм Claude Code уже верен для среды as-is, но ограничен одной машиной и не решает передачу самого session ID между репозиториями — тот же разрыв, что и у остальных систем, просто без реестра, к которому его можно было бы привязать.

Решение о применимости этого к архитектуре Engineer/Researcher не принято здесь — Craft/Engineering граница выдержана.

Полный ответ: `knowledge/evidence/2026-08-12-independent-agent-execution-record.md`; источники — `knowledge/EVIDENCE_INDEX.md`, Batch EXECREC.

## Третий прямой вызов: как зрелые системы различают available/belongs/permitted/recommended для tool-capabilities

Бриф от Engineer, из реального кейса: `ENGINEER/skills/tool-selection/SKILL.md` утверждал, что Exa/Firecrawl/Context7 «не принадлежат Engineer», хотя `claude mcp list` показал их подключёнными в текущей сессии — правило смешало «доступно сейчас» с «принадлежит»/«разрешено»/«рекомендовано для задачи». Сессия прервалась по лимиту API в середине сбора, продолжена по сообщению координатора с того же места, без потери черновых находок.

Две системы прошли планку брифа (≥3 из 4 слоёв, на уровне механизма, прямой fetch официальных источников): **Claude Code/Claude Agent-экосистема** — все 4 слоя подтверждены (available — MCP scope hierarchy `local`/`project`/`user`, `.mcp.json` vs `~/.claude.json`, `claude mcp list` репортит статус независимо от scope; belongs — project-scope `.mcp.json` для репозитория + subagent `tools`/`mcpServers` frontmatter для конкретного саб-агента; permitted — `settings.json` `permissions.allow/ask/deny`, `mcp__server__tool` правила, managed `allowedMcpServers`/`deniedMcpServers`; recommended — `description`-based делегирование у Skills/subagents + MCP tool annotations, явно помеченные спекой как untrusted-хинты, не permission). **LangChain/LangGraph** — 3 из 4 (available — toolkit/`agent_toolkits`; belongs — `tools=[...]`/`bind_tools()`; permitted — `HumanInTheLoopMiddleware(interrupt_on={...})`, отдельный от tools-списка словарь политик по имени тула; recommended — честно не найдено). Дополнительно, частично: OpenAI Agents SDK (belongs+permitted подтверждены через `allowed_callers`/`needs_approval`, available у них не отделяется от belongs — даже hosted tools требуют явного `tools=[...]`) и AWS Bedrock Agents Classic (belongs+permitted через двустороннюю IAM-политику — identity-based на роли агента + resource-based на Lambda с условием на конкретный `agent-id` — но сам продукт помечен AWS как maintenance-mode для новых клиентов). Salesforce Agentforce — честно не подтверждено: основные страницы Help Center вернули 404/не отрендерились в этой сессии, не засчитано доказательством.

Прямая находка для кейса Engineer: `.mcp.json` (project scope) — это и есть техническое поле, которое отвечает на вопрос «принадлежит ли это репозиторию ENGINEER» — Engineer уже сам зафиксировал в своём дневнике, что такого файла нет, а значит Exa/Firecrawl/Context7 подключены как local- или user-scope (машина/пользователь), не как собственность репозитория. Не применимо к кейсу Engineer: subagent `tools:`/`mcpServers:` frontmatter — Engineer не является `.claude/agents/*.md` саб-агентом, это основная сессия/персона, архитектурно на позиции родителя, а не саб-агента.

Полный ответ: `knowledge/CAPABILITY_MAP.md` → «Refinement to Capability 4 (Permissions) — Capability Layering» (2026-08-12); сырое evidence — `knowledge/evidence/2026-08-12-capability-layering-tool-permissions.md`; `knowledge/EVIDENCE_INDEX.md` → Batch LAYERS; портфельная запись — `knowledge/MY_AGENTS.md` → «Engineering Agent», второй cross-reference. Вопрос зарегистрирован только в `ENGINEER/OPEN_QUESTIONS.md` (не в собственном `OPEN_QUESTIONS.md` этого дома) — закрытие записи там, за Engineer.
