# Роутинг

Как агент решает, что читать и что делать под конкретную задачу — не всё сразу, а точечно.

---

## Сначала пойми потребность, потом ищи, чем её закрыть

Не сразу ищи файл — сначала пойми, какого рода потребность у задачи:

| Потребность | Что это | Чем закрывается |
|---|---|---|
| Вспомнить | Уже знаю, но нужно поднять | `MEMORY.md`, `memory/`, `knowledge/*` |
| Проверить | Не уверен, факт под вопросом | внешний источник; если вопрос уже обсуждался — `DECISIONS.md` |
| Исследовать | Нужно разобраться в новом | скилл `reverse-engineering` |
| Внешний контекст | Нужны данные вне дома | подключённый инструмент, если есть (`.mcp.json`) |

Если под потребность нет подходящей возможности — не подменяй её догадкой. Скажи прямо: такой возможности пока нет (та же дисциплина, что и `[УТОЧНИТЬ]` в `HOME.md`).

## Роутинг в knowledge/

Под задачу читай нужный файл, а не всю папку `knowledge/`:

| Сигнал задачи | Что прочитать |
|---|---|
| Синтез: что в целом известно о мире AI-агентных систем | `knowledge/CAPABILITY_MAP.md` |
| Проверить конкретный источник/находку | `knowledge/EVIDENCE_INDEX.md` |
| Найти механизм для Obsidian Builder | `knowledge/MECHANISM_LIBRARY.md` |
| Сверить находку с уже известным о собственных агентах владельца | `knowledge/MY_AGENTS.md` |
| Сырой материал полного разбора конкретного проекта | `knowledge/cases/` |
| Сырое, попроектное evidence (не синтез) | `knowledge/evidence/` |
| Пришёл Research brief от другого агента — сверить формат перед стартом | `knowledge/research-brief-template.md` |
| *(специализация дописывает свои строки сюда)* | |

## Меню vs прямой запрос

При первом содержательном сообщении, если специализация задаёт набор режимов и задача не названа явно — покажи меню выбора (`AskUserQuestion`).

**Пропусти меню**, если:
- задача уже названа словами (даже неточно) — работай напрямую;
- запрос — «продолжи» / «доделай» — открой последний дневник и продолжай оттуда.

## Как скилл понимает, что его вызвали

Каждый скилл описывает в своём `description`, на какие формулировки он откликается. Совпадают слова запроса с описанием — скилл подключается сам, без явной команды. Это отдельный от меню способ попасть к тому же результату — оба варианта равноправны, ведут к одному месту.

## Одно правило, которое легко нарушить

Не смешивай два механизма маршрутизации в одном месте: либо решение принимает фиксированное меню, либо — совпадение с `description` скилла. Если оба работают одновременно над одним и тем же выбором, поведение станет непредсказуемым.
