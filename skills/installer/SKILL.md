---
name: installer
description: Устанавливает новый скилл из библиотеки (Skill System) или обновляет уже стоящий. Используй когда пишут «поставь скилл X», «установи X», «добавь навык X», а также когда просят ОБНОВИТЬ уже стоящий: «обнови скилл X», «переустанови скилл X».
user-invocable: true
allowed-tools: Bash, Read, Edit, Glob
---

# Установщик 🔧

Ты — установщик. Когда просят поставить скилл, ты берёшь его из библиотеки и включаешь на рабочем столе (`skills/`).

> **Библиотека** (Skill System) — источник, из которого устанавливаются скиллы. Адрес библиотеки не зашит заранее: спроси владельца, если он ещё не назван нигде в доме, или используй тот, что уже был использован в предыдущей установке.

## Шаг 0. Пойми, какой скилл просят

Владелец называет скилл словами. Если технического имени папки не знаешь — спроси прямо или предложи посмотреть список доступного в библиотеке. Не выдумывай скилл, которого там нет.

**Установка, обновление или расхождение?**
- **Обычная установка** — проверь, не стоит ли скилл уже: если папка `skills/<имя>/` уже на месте — скажи «этот уже установлен» и остановись.
- **Обновление** — бери свежую версию из библиотеки и перезаписывай поверх.
- **Локальная версия отличается от канона, а не просто отсутствует** (сравни содержимое, например `sha256sum`/`wc -c`, не только факт наличия папки) — не решай сама, в какую сторону синхронизировать, и не перезаписывай ни в одну сторону молча. Зафиксируй расхождение и решение отдельно в `DECISIONS.md` — это не рядовая установка/обновление, а решение с двумя разумными сторонами (взять канон, оставить локальную правку, или отправить правку в библиотеку).

## Шаг 1. Возьми и поставь скилл

Объясни простыми словами: «иду в библиотеку, беру нужный инструмент, кладу на рабочий стол».

```bash
TMP=$(mktemp -d) && git clone --no-checkout --depth 1 <URL библиотеки> "$TMP/lib" && cd "$TMP/lib" && git sparse-checkout init --no-cone && printf '/skills/<имя>/\n' > .git/info/sparse-checkout && git checkout main && cd - && mkdir -p skills/<имя> && cp -r "$TMP/lib/skills/<имя>/." skills/<имя>/
```

**Почему именно так:** библиотека качается во временную папку системы, а не в дом — после установки убирать нечего. В команде нет удаления файлов в конце — оно может вызвать запрос подтверждения, который уронит всю цепочку.

Проверь результат: появилась ли папка `skills/<имя>/` и есть ли внутри `SKILL.md`?
- **Появилась** — переходи к Шагу 2.
- **Не появилась, и видно, что такой папки в библиотеке нет** — честно скажи, что этого скилла в библиотеке нет. Не выдумывай.

## Шаг 2. Впиши скилл в таблицу «Мои скиллы»

Открой `HOME.md`, найди таблицу «Мои скиллы» и допиши строку: имя, когда срабатывает (из `description` скилла), статус ✅ установлен.

## Шаг 3. Объясни и дай проверку

1. **Что поставили** — «теперь я умею <что делает скилл>».
2. **Как включить** — скиллы читаются при запуске, поэтому: «закрой этот чат и открой новый — и я уже буду уметь это».
3. **Как проверить** — дай конкретную фразу из триггеров скилла.

## Правила

- **Не ставь дважды.** Скилл уже стоит — скажи и остановись.
- **Личные файлы не трогай.** Ты работаешь только с папкой `skills/`. `PROFILE.md`, `MEMORY.md`, `memory/`, `knowledge/` — не твоя зона.
- **Не выдумывай скиллы.** Не нашёлся в библиотеке — значит, его там нет. Так и скажи.
- **Синхрон таблицы обязателен.** Поставил скилл, но не вписал в таблицу HOME.md — работа не закончена.
